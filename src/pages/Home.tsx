import React from 'react'
import { Button } from '../components/Button'

export const Home = (): JSX.Element => {
  return <div className="bg-green-500"><Button onClick={() => {}} buttonText="Hello world"/></div>
}
